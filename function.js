#!/usr/bin/node

// in the early days of JS

function awesomeFunction(coolThings) {
	let amazingStuff;

	// ..

	return amazingStuff;
} // This is called a function declaration because it appears as a statement by itself

// possible ways of defining a function variable these days...

//can also be 'let' or 'const'
var moreAwesomeFunction = function (coolThings) {
	let amazingStuff;

	// ..
	
	return amazingStuff;
}; // This is called a function expression that is assigned to a variable.
