#!/usr/bin/node

var names = [ "Ezekiel", "Samuel", "Comfort", "Victoria" ];

console.log(names.length); // length of the array

console.log(names[0]); // element at index 0 of the array, which is the first element

console.log(names[1]); // element at index 1 of the array, which is the second element
